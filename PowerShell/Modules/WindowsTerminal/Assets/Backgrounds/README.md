# Windows Terminal backgrounds

Images in this directory are bundled assets exposed by `Get-WindowsTerminalBackgrounds`.
Use a background by its filename without the extension:

```powershell
winterm backgrounds
winterm background dragonbattle_span
```

Supported formats are JPG, JPEG, PNG, and GIF. Record the source and redistribution
license here when adding third-party images.
